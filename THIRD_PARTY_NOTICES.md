# Third-party notices

This distribution uses exactly `patronus-ark 0.1.6`. The corresponding crate
currently published through crates.io declares `GPL-3.0-only` in its package
metadata and includes its own `LICENSE`, `LICENSE-COMMERCIAL.md`, and `NOTICE`
files. Casdo Labs GmbH / Patronus owns ARK and has authorized its inclusion in
this Apache-2.0 distribution under different terms. This authorization applies
to this distribution; it does not alter or relabel the metadata of the
separately published crates.io artifact.

The repository's automated license policy records `patronus-ark` as the only
package-scoped `GPL-3.0-only` exception. The Patronus Security CLI itself and
its first-party integrations continue to declare `Apache-2.0`.

The embedded Inter and Manrope font subsets remain subject to the SIL Open Font
License. Their notices are provided in `src/dashboard/assets/Inter-OFL.txt` and
`src/dashboard/assets/Manrope-OFL.txt`.

See `Cargo.lock` for the exact Rust dependency versions and source metadata used
by this build. This notice does not attempt to restate the license metadata of
every transitive dependency.
